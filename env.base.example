# Base Environment Configuration (env.base)
# Copy this file to env.base and fill in your actual values
# This file contains common configurations for all stages

# =============================================================================
# COMMON CONFIGURATION (Shared across all stacks)
# =============================================================================

# AWS Region where resources will be deployed
AWS_REGION=us-east-1

# Your registered domain name (must have Route53 hosted zone)
DOMAIN_NAME=your-domain.com

# SSL Certificate ARN from AWS Certificate Manager
# Get this from: aws acm list-certificates --region us-east-1
CERTIFICATE_ARN=arn:aws:acm:us-east-1:123456789012:certificate/your-certificate-id

# =============================================================================
# BACKEND STACK CONFIGURATION
# =============================================================================

# Application name (used for resource naming)
APP_NAME=poc-backend

# Port your FastAPI application runs on
APP_PORT=8000

# Container memory allocation (in MB)
APP_MEMORY=512

# Container CPU allocation (in CPU units, 256 = 0.25 vCPU)
APP_CPU=256

# Default container image tag
CONTAINER_IMAGE_TAG=latest

# =============================================================================
# FUTURE STACKS (Add configurations for other stacks here)
# =============================================================================

# Example: Database Stack Configuration
# DB_INSTANCE_TYPE=db.t3.micro
# DB_STORAGE_SIZE=20

# Example: Cache Stack Configuration  
# CACHE_NODE_TYPE=cache.t3.micro
# CACHE_NUM_CACHE_NODES=1 